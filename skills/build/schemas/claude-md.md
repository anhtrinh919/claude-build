# CLAUDE.md — [Project Name]

This doc is a starting point, not a schema. Grows freely.

## Must-have

- `.build-state.json` holds the step. If present, `/build` orchestration is active — invoke the `/build` skill (except for trivial one-off or unrelated tasks).
- One sentence: what this product is, for whom.